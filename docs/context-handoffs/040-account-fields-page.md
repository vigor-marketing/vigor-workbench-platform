# 040 – 新增「账号字段」独立页面（管理新增/编辑账号弹窗的字段选项，含新增和删除）

## 需求
「我需要有一个单独的页面来管理编辑，新增账号的字段，包含新增和删除」+「不动原来的账号与权限的页面」

## 方案
- 新增独立页面「账号字段」（路由 /admin/account-fields，导航位于「账号与权限」之后，icon layers）。
- 管理「账号 新增/修改」弹窗的字段选项：**部门选项 / 小组选项（按部门）/ 岗位选项（内置只读 + 自定义）/ 部门主管岗位规则**，均可新增、重命名、删除。
- 账号与权限页面**其余部分（卡片、分组、徽标、主管 chip、按组展示等）完全不动**；仅弹窗下拉的数据源切换到该配置（选项内容与组织架构一致，视觉无变化）。

## BFF
- 复用 037 的 `account-fields.service.ts`（存 /var/lib/vigor-workbench/account-fields.json），首次从 org.json 迁移生成；本次新增：小组选项按组织架构排序（销售 V1–V5；采购 一组/二组/质量组）。
- `auth.service.ts` 补回 renameDepartment/renameTeam/renameRole + count*（级联与删除保护）。
- controller：GET/PUT /api/account-fields（PUT 支持 renames 级联 + 删除保护）+ POST account-fields/teams|roles。

## 前端
- `AccountFieldsPage.tsx`（复用 037）：部门/小组/岗位/主管规则四段，本地编辑 +「保存全部修改」。
- 账号弹窗（App.tsx PermissionAdminPage）：部门下拉 = config.departments；小组下拉 = config.teams（按组织排序）；岗位下拉 = 内置 + config.customRoles + 账号中使用岗位；主管规则 = config.headRoles（回退内置默认）；弹窗内联新增小组/岗位 → account-fields 接口。
- styles.css 合并 org-manage 样式；Icon 补回 layers。

## 后续调整（1eefe64）：并入账号与权限页 + 快捷跳转
- 按需求「账号字段的三个管理做成方便跳转」「放到账号权限页面里面，菜单里两个分类分别管理」：
  - 移除独立导航项「账号字段」与路由；字段管理并入「账号与权限」页，页内两个标签：**账号列表 / 字段管理**（.api-tabs）。
  - 「字段管理」标签顶部有**快捷跳转栏**（部门/小组/岗位/主管规则，sticky 置顶），点击平滑滚动到对应区块（区块 scroll-margin-top 130px）；右侧「放弃并刷新」「保存全部修改」+ 未保存提示。
  - 两标签视图都保持挂载（display 切换），切换时未保存的字段编辑不丢失。
- 验证（Playwright）：导航无独立「账号字段」；账号与权限页双标签；默认账号列表（8 部门）；切字段管理 → 4 区块 + 跳转栏；点「主管规则」平滑滚动到位；切回账号列表正常；无 JS 报错。

## 再次调整（7002baa）：去掉跳转，改为子页面切换
- 按需求「不做跳转，做成字段管理下面的单独页面进行管理，需要能删除和管理相关字段」：
  - 移除快捷跳转栏（org-manage-jump）。
  - 「字段管理」下新增**子标签页**：部门 / 小组 / 岗位 / 主管规则 —— 每个是一个独立管理页面，各自支持新增、重命名、删除。
  - 顶部保留操作条（有未保存提示 + 放弃并刷新 + 保存全部修改），保存整份配置；子页切换不丢失编辑。
- 验证（Playwright）：字段管理下 4 个子标签；部门页 8 行 + 新增删除可用；小组页部门选择 + 小组列表；岗位页 19 内置；主管规则页 8 行 + 8 个下拉；临时新增删除部门通过；配置无污染；无 JS 报错。

## 再次调整（7ada040）：每张表单独保存 + 修复更新不生效
- 按需求「主管规则没有更新，岗位无法更新，每一表单独保存，不要有上方的保存全部修改」：
  - **去掉顶部「保存全部修改」**：每个子页面（部门/小组/岗位/主管规则）底部各有一条操作条（未保存提示 + 放弃本表修改 + 保存{本表}）。
  - **修复根因**：字段页保存后未通知父页面刷新，导致账号弹窗的部门主管规则/岗位下拉使用旧数据 → 新增 `onSaved` 回调，保存后同步刷新「账号与权限」页的字段状态。
- 验证（Playwright）：主管规则页改财务部主管岗位为会计 → 保存主管规则 → 账号弹窗选会计时「部门主管」勾选可用（规则已同步）；岗位页新增「测试新岗位X」→ 保存岗位 → 弹窗岗位下拉出现该岗位；无 JS 报错；测试改动已清理（财务部恢复 finance_manager）。

## 再次调整（564756e）：重命名改行内编辑 + 岗位表列出使用中岗位
- 按需求「重命名不要调用系统弹窗」「信息还是不同步，岗位无法重命名和删除」：
  - **重命名改为行内编辑**（点击重命名 → 行内输入框 + 保存/取消，Enter/Esc 支持），不再调用 window.prompt。
  - **岗位表列出使用中的自定义岗位**（如 市场运营组组长/销售支持组组长）：显示 = 配置自定义岗位 ∪ 账号中使用岗位（带使用账号数）；均可重命名（级联账号）与删除（使用中删除有保护提示）。
  - 保存后 `onSaved` 同时刷新账号列表与字段配置 → 弹窗岗位下拉/主管规则即时同步。
- 验证（Playwright）：岗位表列出 3 个自定义岗位（含使用中）；行内输入出现（无对话框）；删除使用中岗位提示「正在被 N 个账号使用，无法删除」；API 级联重命名 市场运营组组长→测试改名岗→还原，monk 账号岗位随之变化；无 JS 报错。

## 再次调整（7f3347a）：系统内置岗位支持重命名/删除/恢复
- 按需求「系统内置的岗位无法进行修改删除和重命名」：
  - 岗位表中的**内置岗位**也可管理：
    - **重命名**：行内编辑，修改显示名称（存 `roleLabels` 覆盖，账号弹窗/卡片/主管规则下拉同步显示新名，账号角色 ID 不变、部门分组不受影响）。
    - **删除**：移入 `disabledRoles`，从账号弹窗可选岗位中移除；有账号使用时弹确认（已有账号保留原岗位，仅无法再分配给新账号）；显示「已从选项移除」。
    - **恢复**：从 disabledRoles 移除，重新可选。
  - BFF account-fields 增 `roleLabels` / `disabledRoles` 字段；账号页 deptOf 增加按角色 ID 回退，改名不影响部门分组。
- 验证（Playwright）：会计 → 重命名"核算会计"→ 删除（显示已从选项移除）→ 保存 → 弹窗岗位下拉不再出现；恢复 + 改回"会计" → 保存 → 配置还原（roleLabels/disabledRoles 清空）；无 JS 报错。

## 再次调整（2fc2d4e）：修复字段配置后账号无法显示
- 按需求「字段设置好，无法调用」：
  - 根因：账号页部门分组列表为硬编码的 8 个部门，用字段管理新增的部门创建账号后，该账号所在部门分组不渲染 → 账号"消失"。
  - 修复：账号页分组列表 = 内置 8 部门 ∪ 字段配置中的部门 ∪ 「其他」，新建部门配置的账号可正常显示。
- 验证（Playwright）：字段管理新增部门"测试部门X"+小组"资金组"+岗位"资金专员" → 账号弹窗下拉均可选 → 用这些字段创建账号成功 → 账号卡片出现在"测试部门X"分组；测试数据已清理还原（8 部门、财务部仅财务部组、customRoles 空）。

## 再次调整（0021b4b）：账号表单与字段配置强关联
- 按需求「字段设置好之后，账号编辑依旧不显示」「能不能做强关联」：
  - 账号表单（新增/编辑）下拉**只读取字段配置**（移除对组织架构数据的回退）：部门 = 配置部门；小组 = 配置小组（按组织排序）；岗位 = 内置(含改名/禁用) + 配置自定义 + 账号使用岗位；主管规则 = 配置。
  - **切回「账号列表」标签时自动重新拉取字段配置与账号**——配置改动后，编辑弹窗立即同步显示，杜绝旧数据。
  - 账号页部门分组 = 内置 8 部门 ∪ 配置部门 ∪ 其他（新配置部门可显示账号）。
- 验证（Playwright）：用配置的"测试编辑部门"+市场运营组组长 建号 → 编辑弹窗正确显示部门/岗位；切部门为销售部 → 小组下拉为配置的 V1–V5；无 JS 报错；测试数据已清理还原（8 部门）。

## 再次调整（1ba0a47）：字段配置同步调整组织架构后端数据
- 按需求「前端设置好后段的数据没有调整」：
  - 保存账号字段配置（部门/小组）时，**同步调整 org.json 组织架构数据**：先重命名、再新增、后删除（删除仅当组织内无人员，避免误删人员数据）。
  - 因此字段管理里新增/重命名/删除部门、小组，组织架构图与人员选择器同步反映。
- 验证：PUT 新增"测试同步部"+一组/二组 → org tree 出现该部门与小组；移除后 org 同步删除；无 JS 报错；测试数据已清理。

## 再次调整（0b0a70a）：组织架构图岗位显示与账号权限一致
- 按需求「组织架构和账号权限数据不一致」：
  - 根因：账号字段中改名的内置岗位（质量组→质量组组长、市场组→市场运营组组长、销售支持组→销售支持组组长）只在账号页生效，组织架构图显示 org.json 旧名。
  - 修复：组织架构图加载账号字段配置，岗位显示套用同一改名映射（roleText：org 岗位标签 → 内置岗位 ID → roleLabels 覆盖 → 显示），人员卡片/负责人/总经理节点岗位名与账号页一致。
- 验证：陈炳屾/张菲/徐振兴 在组织架构图与账号与权限两页岗位名完全一致（市场运营组组长/销售支持组组长/质量组组长）；无 JS 报错。

## 再次调整（3865ba1）：组织架构与账号权限岗位映射全面一致
- 按需求「miya的岗位信息也改了，也不一致，你仔细检查一下」「确保所有的映射正确」：
  - 全面核查发现 8 处岗位不一致（org vs 账号）：miya 行政专员/总经理助理、vic 销售员/采购员、shane 质量组/质量组组长、andy+hunter 质量组/质量检验员、robin 销售支持组/销售支持组组长、garen 销售支持组/销售支持人员、monk 市场组/市场运营组组长。
  - 修复：
    - 组织架构图**人员岗位以账号为准显示**（账号角色 ID → 改名覆盖 → 内置标签 → 原样；无账号回退组织岗位），保证两页映射恒一致（含改名/自定义岗位）。
    - 修正 vic 账号为 销售员/销售部/V5(钢铁战士)（与组织架构一致）。
  - 验证（Playwright 全量）：组织架构图 48 个人员芯片与账号页 48 张卡片岗位**逐人核对 0 不一致**；无 JS 报错。

## 验证（部署后）
- account-fields 首次生成：8 部门、采购部 = 采购一组/二组/质量组（排序正确）、销售部 V1–V5、主管规则含 销售支持组组长/市场运营组组长。
- UI 全链路（Playwright）：账号弹窗采购部小组下拉三组有序；账号页采购部子分组不变；字段页新增小组「资金组」+岗位「资金专员」+改财务部主管岗位 → 保存 → 弹窗财务部出现资金组、会计主管勾选禁用、资金专员主管勾选可用；无 JS 报错；测试改动已清理还原。
- 提交：`df0f36e`（功能）、`6502dd9`（编译修复）。部署已生效。
