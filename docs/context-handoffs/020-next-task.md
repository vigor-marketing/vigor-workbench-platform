# 020 — 服务端账号、岗位与权限基线

更新时间：2026-08-13

## 本轮完成

- 腾讯云 CVM（`ins-59qdrwy7`）工作台 BFF 已部署服务端认证：`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/session`。
- 会话使用 `HttpOnly`、`SameSite=Lax` Cookie；不再信任 `X-Demo-Role` 请求头。
- 待办、应用清单、AI 陪练桥接、服务库与应用授权管理均改为由服务器会话校验。
- 管理员账号接口：`GET /api/admin/users`、`POST /api/admin/users`、`PUT /api/admin/users/:id`；密码哈希仅保存在 `/var/lib/vigor-workbench/users.json`（0600），接口不会返回密码。
- 个人改密接口：`POST /api/auth/change-password`。
- 工作台登录页已改为调用服务器会话，移除了前端展示的内测账号密码。个人账号与“账号与权限”页面已改为调用服务端接口。
- 已将此前确认的岗位补齐到 BFF：销售组长、项目跟进员、采购组长、采购员、质量组、人力总监、行政专员、会计、船务经理/操作员、销售支持、市场组等；三个已接入应用的基础岗位访问范围已同步。

## 验证结果

- 未登录访问 `/api/apps`、`/api/admin/users`：401。
- 管理员登录后：会话、待办、账号列表、三个应用清单均为 200。
- BFF 与前端均已编译；`vigor-workbench-bff.service`、`vigor-workbench.service` 为 active。
- 实际 IP Host（`1.15.91.150`）同源接口已验证可用。

## 已知边界 / 下一步

- 工作台左侧应用导航仍保留旧的前端 `demo-auth` 权限映射；服务端 API 已强制校验，但下一轮应将“应用可见性与操作级授权”也迁移为服务端持久化权限表，移除该本地映射。
- 当前账号初始管理员沿用既有账号；应先通过“个人账号”页面更换为仅用户知晓的强密码。
- 用户要求所有程序仍保持 HTTP/IP 访问；Cookie 未设 `Secure` 仅为匹配此决定，正式含敏感业务数据前应重新评估。
- 依用户“逐个完成”的总清单，下一个建议项目：统一身份接入其余三个小程序（编码器、提成、知识库），移除各自独立/模拟身份状态。

## 约束

- 不停止或修改 AI 陪练 3000 服务，除非用户明确授权。
- 不暴露 BFF、数据库或任何 API 密钥；AI 密钥继续由工作台代理托管。
- 所有部署在腾讯云 CVM；资料最终落 COS，不使用 CloudBase。
- 后续接近上下文压缩时，先更新此交接文件并创建下一编号交接任务。
