# 027 — 统一工作台响应式基础与公共页面适配完成

更新时间：2026-08-14

## 本轮完成

- 用户确认响应式范围为整个 Vigor 统一工作台及所有应用；销售管理与采购 CAD/3D AI 助手独立任务已同步该要求。
- 全局壳层已支持 960px 以下抽屉式导航，包括键盘可操作开关、遮罩关闭、Esc 关闭、导航后自动收起、触屏点击区和减少动画设置。
- 公共内容页已补齐响应式约束：工作总览、待办中心、个人账号、账号与权限、应用岗位权限、API 服务/授权和接入设置。
- 已处理信号栏、待办行、应用目录、账号表单、权限卡片、API 列表与操作区在窄屏、高倍率缩放及触屏下的重排和文本换行。
- 销售管理独立任务的响应式样式已先完成并保留；本任务基于其最终 SHA-256 合并，没有覆盖销售专属区块。

## 部署与验证

- CVM：`ins-59qdrwy7`（ap-shanghai）；公网入口：`http://1.15.91.150/`。
- SSH：`ubuntu@1.15.91.150`，密钥仅引用本机路径 `/Users/monk/Downloads/cbs069791292101.pem`，不得复制私钥内容。
- 前端源：`/opt/vigor-workbench-pilot/app/src/App.tsx`、`/opt/vigor-workbench-pilot/app/src/styles.css`。
- 最终 `styles.css` SHA-256：`d8b138ac78a468a370fc7d3346993cb4b9c07acc69bcdf92fa79804dfba57aef`。
- TypeScript 与 Vite 构建成功；响应式 CSS 和导航组件均已进入 `dist` 产物。
- 独立浏览器验证 1920×1080、1280×800、768×1024、390×844 登录页，均无横向溢出或裁切。
- BFF 与 Nginx 保持 active；公网入口返回 200；3000 端口持续监听且未修改。

## 备份与恢复

- 全局壳层备份：`/opt/vigor-workbench-pilot/backups/responsive-shell-20260814-154000`。
- 公共内容适配备份：`/opt/vigor-workbench-pilot/backups/content-responsive-20260814-155000`。
- 销售响应式备份由销售任务维护：`/opt/vigor-workbench-pilot/backups/sales-responsive-20260814-153019`。

## 已知验证边界

- Chrome 控制通道读取已登录工作台标签页时持续超时，因此尚未完成认证后所有公共页面的截图矩阵。
- 无凭据浏览器中的 `/api/auth/session` 与 `/api/todos` 401 属于未登录状态；持续待办轮询为既有行为，本轮未越界修改。缺少 favicon 的 404 亦未处理。

## 下一项建议

1. 浏览器已登录控制通道恢复后，完成认证后公共页面与嵌入应用的多视口截图验收。
2. 按应用逐项检查嵌入内容自身的响应式能力；业务应用修改继续由各自独立维护任务负责。
3. 采购 CAD/3D AI 助手继续保持计划模式，先确认零件类型、输出格式、审核人和精度要求。

## 固定边界

- 不停止或修改 AI 销售陪练 3000 服务。
- HTTP/IP 仅用于内测，不录入真实敏感经营数据、图纸、账号或密钥。
- 所有应用部署于腾讯云 CVM，资料最终进入用户 COS；不使用 CloudBase。
- 不在聊天、代码、日志或交接文件中暴露 BFF、数据库、COS、API 或 SSH 私钥内容。
- 重大业务变更继续逐项确认。

