# 028 — CAD/3D 方向确认；认证后多视口截图验收完成

更新时间：2026-08-17

## 本轮完成

### 采购 CAD/3D AI 助手（保持计划模式，未写代码）
- 零件类型：暂不锁定，先做单类零件概念验证（POC）；
- 输出格式：先只做在线预览，导出格式（STEP/STL/GLTF/图纸）验证后再定；
- 审核人：采购经理；
- 精度要求：验证后再定，不预先锁定公差。

### 认证后多视口截图验收（已完成）
- 方式：本地 Vite 前端 + Playwright（完整 Chromium 无头）+ 对 `/api/*` 做 mock（不接触真实凭据与真实数据），对 9 个认证页面做 5 视口截图。
- 视口：1920×1080、2560×1600、3440×1440（21:9）、768×1024（平板）、390×844（手机）。
- 结果：45 屏「横向溢出 = 0」「容器内水平裁切 = 0」。唯一越界项为 ≤960px 收起在画布左侧的抽屉侧栏（预期行为）。
- 覆盖页面：工作总览、待办中心、个人账号、账号与权限、应用岗位权限、API 服务库、应用 API 授权、接入设置、销售管理。

### 部署端只读复验（未改线上）
- 工作台登录页（`http://1.15.91.150/`）与嵌入应用 ai-sales-coach（`/apps/ai-sales-coach/`）各 5 视口，横向溢出 = 0。
- `product-encoder` / `sales-commission` / `knowledge-base` 返回 401（需桥接鉴权），其自身响应式由各自独立维护任务负责；`sales-management` 即工作台自身页面，已在上方覆盖。

## 本轮发现并修复的 install 回归（由 sync 提交引入）
1. `pnpm-workspace.yaml` 丢失 `packages: - apps/*`，导致 `pnpm install` 不识别 `apps/bff` 工作区 → 已恢复。
2. `pnpm-lock.yaml` 残留 `tencentcloud-sdk-nodejs@4.1.286`，但 `apps/bff/package.json` 已不含该依赖，导致 `pnpm install --frozen-lockfile` 失败 → 已重生成 lockfile 移除残留。

## 已知验证边界
- 本会话模型不支持图像输入，故截图验收采用「横向溢出 + 容器内裁切」程序化指标（均 0），未做像素级人工复核；45 张本地截图与 10 张部署端截图已落盘，可交具备图像能力的通道复核。

## 下一项建议
1. 由具备图像能力的通道对截图做像素级人工复核。
2. 补齐 `product-encoder` / `sales-commission` / `knowledge-base` 三个嵌入应用的响应式验收（需桥接鉴权）。
3. CAD/3D 助手进入 POC 前，先确认概念验证的具体零件对象与验收标准。

## 固定边界
- 不停止或修改 AI 销售陪练 3000 服务。
- HTTP/IP 仅内测，不录入真实敏感经营数据、图纸、账号或密钥。
- 所有修改先提交并推送到 GitHub，确认 main 更新后再部署 CVM。
- 不在聊天、代码、日志或交接文件中暴露 BFF、数据库、COS、API 或 SSH 私钥内容。
- 重大业务变更继续逐项确认。
