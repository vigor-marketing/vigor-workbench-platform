# 024 — 销售模块完成，待下一项确认

更新时间：2026-08-14

## 本轮完成

- 销售核心 BFF 已部署：客户、商机、报价、合同、回款、项目六类台账；数据文件位于 CVM 的 `/var/lib/vigor-workbench/sales.json`，权限模式为 0600。
- 正式报价通过审批后生成含版本的编号；合同仅能来自已审批报价；项目仅能来自已确认 CI 的合同且必须指定跟进员。
- 工作台已增加“销售管理”入口和同风格操作页面，支持创建六类业务记录，支持提交报价、审批报价、确认 CI，实时读取销售台账。
- 已通过管理员会话与完整演示流程验证。演示资料均标记“非真实”，当前台账共 1 套演示链路。
- 页面 UI 使用工作台既有黑红、紧凑台账风格；机械 UI 检测结果为空；前端、BFF 均构建成功，BFF 服务 active。
- 用户确认保留演示数据，并要求重做销售页面 UI。已于本轮完成重做：销售页面改为“销售总览 / 客户与商机 / 报价与合同 / 回款与项目”四个业务子页面；总览用于判断下一步，子页面分别呈现对应台账、创建入口和流程动作。
- 已移除工作台外层接入页标题，避免与销售页面标题重复；桌面实测可登录、读取演示台账、切换“报价与合同”子页面并显示正式报价、合同和创建表单。前端与 BFF 最后构建成功，BFF service active。

## 当前访问与安全基线

- 销售管理岗位：总经理、分管销售副总、销售经理、销售组长、销售员、项目跟进员。
- 所有应用均继续部署在腾讯云 CVM；不使用 CloudBase；资料最终进入用户 COS。
- 仍是 HTTP/IP 内测入口。不得录入真实客户、合同、回款、证件或 API 密钥，直至用户另行确认生产安全措施。
- 不停止或修改 AI 陪练 3000 服务；不暴露 BFF、数据库、COS 或 API 密钥。

## 需用户逐项确认的下一工作

1. 是否保留当前销售演示数据，还是清空后等待真实业务启用。
2. 销售模块下一项应优先做哪一项：销售小组数据隔离、报价审批层级、用印申请、合同电子/纸质归档。

## 续接规则

- 接近上下文压缩前先更新本文件，并新建顺序号 `025-next-task.md`。
