# Vigor 工作台设计系统

## 设计意图

工作台采用“运营调度台”而非传统数据大屏：先看到应做的事，再看到各业务模块是否可用，最后进入具体系统。它服务于日常办公协同，不伪装成实时经营 BI。

## 视觉语言

- 基底：暖灰纸面 `#f3f3ef`，模块面 `#fafaf7`；
- 操作与结构：深蓝灰 `#15202c`、`#234861`；
- 强调：琥珀 `#eebd59`（待关注）、砖红 `#bd4f38`（逾期或风险）、青绿 `#2d7866`（完成）；
- 排版：中文使用 Noto Sans SC；编号、环境与状态使用 DM Mono；
- 结构：固定侧栏、页面顶部细线、台账式分隔行、序号与紧凑状态标签。

## 组件约定

- `signal-bar`：首页唯一的数量总览，聚焦待办、审批、风险；
- `todo-row`：每项均有可操作完成状态、来源、截止时间；
- `module-row`：应用状态与入口合并，不使用无意义的统计卡片；
- `state-card`：未配置、无权限、载入失败的明确状态，不显示空白 iframe；
- `platform-notice`：说明系统边界的浅色提示区，使用顶部细线而非侧边彩条。

## 可访问性与响应式

- 交互控件保留文字或 `aria-label`；
- 颜色不单独表达状态，配合文字标签；
- 窄屏下隐藏次级部门导航，待办行可换行，嵌入模块仍保持独立入口。

## 不应使用

- 渐变背景、玻璃拟态、无意义 KPI 卡片墙；
- 把前端的菜单隐藏视为权限控制；
- 不受控的外站 iframe；
- 用视觉标记代替业务的审批、提醒或审计规则。
